  <nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
       <ul class="nav navbar-nav">
        <li class="active"> <a href="index"><img src="logo2.png" width="110px"/> </a>
          <a class="button" href="about"> About </a>
          <a class="button" href="createAccount"> Create an Account </a>
          <a class="button" href="howtoUse"> How to Use PDF Hub </a>
          <a class="button" href="myLibrary"> My Library </a>
      </ul>
    </div>
    <form class="search-bar2" action="/search" method="POST">
      <input class="input2" type="text" placeholder="Search for Textbook by ISBN/Title" name="search">
      <button class="search-icon2"><img src="magnifying_glass.png" width="20px"/></button>
  </form> 
  </div>
</nav>

  <div class="jumbotron text-center">
    <h1> My Library <br /> </h1>
  </div>
  <div class="container">
      <div class="row">
          <nav class="navbar navbar-inverse">
              <div class="container-fluid">
                <div class="navbar-header">
                   <ul class="nav navbar-nav">
                    <li class="active">
                      <div class = "row">
                        <div class="column">
                          <form action="/myLibrary" method="POST">
                            <input type="hidden" name="sortType" value="title">
                            <button class="button-setting"> Sort By Title </button>
                          </form>
                        </div>
                        <div class="column">
                          <form action="/myLibrary" method="POST">
                            <input type="hidden" name="sortType" value="author">
                            <button class="button-setting"> Sort By Author </button>
                          </form>
                        </div>
                        <div class="column">
                          <form action="/myLibrary" method="POST">
                            <input type="hidden" name="sortType" value="isbn">
                            <button class="button-setting"> Sort By ISBN </button>
                          </form>
                        </div>
                      </div>
                      </li>
                  </ul>
                </div>
              </div>
            </nav>
      </div>
    <div class="row">
        {{#each library}}
        <div class="column3">
          <div class ="row">Title: {{this.title}} </div>
          <div class = "row">ISBN: {{this.isbn}} </div>
          <div class = "row">Author: {{this.author}} </div>
          <div class = "row">
            <form action="/download" method="POST">
              <input type="hidden" name="fileName" value={{this.fileName}} />
              <button class="submit-button2">Download Here</button>
            </form>
          </div>
          {{#if this.numReviews}}
          <div class = "row">
            Average Review: {{this.average}}
          </div>
          {{/if}}
          <br />
          <div class = "row">
          <form action="/myLibraryRating" method="POST">
            <input type="hidden" name="book" value={{this.isbn}} />
            <button class="submit-button2">Submit Review</button>
          </form>
          </div>
          </div>
        {{/each}}
    </div>

  
  
